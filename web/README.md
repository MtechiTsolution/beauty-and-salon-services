# Deprecated

This folder is the **old single-page app** (customer + admin in one Vite project).

Use the split apps instead:

- Customer booking: `../customer` (port **5173**)
- Admin dashboard: `../admin` (port **5174**)

See the [root README](../README.md) for setup.
